global:
  scrape_interval: 15s

scrape_configs:
  - job_name: payment-gateway
    static_configs:
      # The gateway runs as a compose service, reachable by name on the shared network.
      # If you instead run the API on the host via `dotnet run`, use host.docker.internal:5067.
      - targets: ['payment-gateway:5067']
